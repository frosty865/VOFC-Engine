{
  "name": "vofc-engine",
  "version": "1.0.0",
  "description": "VOFC Engine - Vulnerability Options for Consideration Engine with AI-powered assessment generation",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter vofc-viewer dev",
    "dev:backend": "pnpm --filter backend dev",
    "dev:viewer": "pnpm --filter vofc-viewer dev",
    "dev:all": "powershell -Command \"Start-Process powershell -ArgumentList 'pnpm run dev:backend'; Start-Process powershell -ArgumentList 'pnpm run dev:viewer'\"",
    "build": "pnpm --filter vofc-viewer build",
    "start": "pnpm --filter vofc-viewer start",
    "embed": "node vofc-viewer/apps/backend/server/services/knowledge/embeddingPipeline.js"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "multer": "^2.0.2",
    "node-fetch": "^3.3.2",
    "pdf-parse": "^2.4.3"
  }
}
